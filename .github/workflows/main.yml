on:
  push:
    branches:
    - main

jobs:
  my_job:
    name: deploy to production
    runs-on: self-hosted
    steps:
    - run: git pull
    - run: dotnet run -e ASPNETCORE_ENVIRONMENT=development

